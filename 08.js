// typeof Operators

let firstName = 'dib';
let age = 22;

// console.log(firstName ,typeof (firstName));
// console.log(typeof (age));

// console.log(age + "");
// console.log(typeof age, age);
// console.log(typeof (age + ''));
age  = age + ""
// console.log(typeof age, age);

// data types

// convert numbers into string :

//Trick to convert a number to a string (using empty string)

// #When a number is concated with a string the number changes to a string 
//So by concating a number or adding a number with a string , try an empty string the number can be converted to a string

// convert string to numbers :

//Trick to convert a string to a number (using empty string)

//# To convert a string to number add a plus + sign before the string and the string will be converted to a number .

// let myStr = "34";
// console.log(typeof myStr, myStr);
// myStr = +myStr;
// console.log( typeof myStr, myStr);

