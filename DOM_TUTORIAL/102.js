// DOM
// document object model
// overview
// how to use
// deep study

// brower will look at the structure of the html file and will make a object.
// That object is called the document(OBJECT => DOCUMENT).

// Browser will make a object called document.
// Then the browser will add this object inside the window object.
// So the document will become a property inside the window object.

// Note: All the elements in the html file are present in the document named(namak) object.

// Human readable representation
// console.log(window.document);
// console.log(this.document);
// console.log(document);

// Javascript object representation(direct)
// console.dir(window.document);
// console.dir(this.document);
// console.dir(document);

