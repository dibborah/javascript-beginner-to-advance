// innerHTML

// const headline = document.querySelector(".headline");
// console.log(headline.innerHTML);
// headline.innerHTML = "<h1>Inner HTML changed</h1>";
// headline.innerHTML += "<button class=\"btn\">Learn More</button>";// += add the previous content and then assign the new content // Keeps the previous content doesn't overrides the previous content
// In the above line when the class was initiall wrote in "" (double quotes) error was thrown since we donot use double quotes inside string
// To use double quotes ("") inside string we use backslash(\).Write \ and then write the quotes .
// Then error won't be thrown
// \" ===> "
// console.log(headline.innerHTML);