// select element using get element by id

// console.log(document.getElementById("main-heading"));// It seems in the console that it is returning an html element but actully it is returning an object.The object is the representation of the html element so it looks as if it is the html element itself
// console.dir(document.getElementById("main-heading"));// The browser have make the element that we are targeting via the id a object and stored that in the document named object and displays it in the console

// const mainHeading = document.getElementById("main-heading");
// console.log(mainHeading);
